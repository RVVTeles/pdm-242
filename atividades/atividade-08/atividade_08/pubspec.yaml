name: atividade_08
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8
  flutter_form_builder: ^7.0.0 # Verifique a versão mais recente
  cpf_cnpj_validator: ^2.0.0 # Adicionando a dependência do CPF/CNPJ
  intl: ^0.17.0 # Adicionando a dependência intl para formatação de datas

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true